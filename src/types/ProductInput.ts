export type ProductInput = {
  title: string;
  description: string;
  whatsapp: string;
  active: boolean;
  createdAt: number;
  ownerId: string;
  ownerName: string;
  imageUrl?: string;
};
